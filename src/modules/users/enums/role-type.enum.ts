export enum RoleType {
  ADMIN = "admin",
  COORDENADOR = "coordenador",
  /**
   * Papel legado em transição.
   * Deve existir apenas por compatibilidade operacional temporária.
   */
  NUGECID_OPERATOR = "nugecid_operator",
  USUARIO = "usuario",
}
